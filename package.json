{
  "name": "web-engineering-ilv-1st-playground",
  "version": "1.0.0",
  "description": "This repository is designed as the foundation for coding playgrounds in the Web Engineering course. It offers a structured space for experimenting with and mastering various web development technologies and practices. The project is based on [this](https://developer.mozilla.org/en-US/docs/Learn/Accessibility/Accessibility_troubleshooting) repository from MDN.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "type-check": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.js",
    "lint:fix": "eslint . --ext .ts,.js --fix",
    "format": "prettier --write \"**/*.{ts,js,json,css,md,html}\"",
    "format:check": "prettier --check \"**/*.{ts,js,json,css,md,html}\"",
    "build:types": "tsc --noEmit",
    "build:lint": "npm run lint",
    "build:clean": "rm -rf dist",
    "build:assets": "vite build",
    "build": "npm run build:clean && npm run build:types && npm run build:assets",
    "build:production": "npm run build:clean && npm run build:types && npm run lint && npm run build:assets",
    "build:dev": "npm run build:clean && npm run build:assets",
    "preview": "vite preview",
    "serve": "vite preview"
  },
  "keywords": [
    "wildlife",
    "website",
    "typescript",
    "vite"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^7.1.10",
    "@vitejs/plugin-legacy": "^7.2.1",
    "typescript": "^5.9.3",
    "@types/node": "^24.8.1",
    "eslint": "^9.38.0",
    "@eslint/js": "^9.38.0",
    "@typescript-eslint/eslint-plugin": "^8.46.1",
    "@typescript-eslint/parser": "^8.46.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "prettier": "^3.6.2",
    "globals": "^16.4.0"
  }
}
